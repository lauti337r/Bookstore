{% extends 'default/index.html.twig' %}

{% block title %}Hello {% endblock %}

{% block body %}
    <img src="{{ user.profilepic }}" width="10%"><br><br>
Welcome, {{ user.firstname }} <br>
    Role: {{ user.role }}<br>
    E-mail: {{ user.email }}<br>
    Linked with: {% if user.googleId is not null %} Google{% endif %}{% if user.facebookId is not null %} Facebook {% endif %}

{% endblock %}